# .env.example
# Exemplo de arquivo de configuração de variáveis de ambiente.
# Renomeie para .env e configure os valores.

# Define o tamanho máximo de conteúdo permitido para uploads (em bytes).
# Por exemplo, 10 * 1024 * 1024 = 10 MB
MAX_CONTENT_LENGTH=10485760
